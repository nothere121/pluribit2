{
  "name": "pluribit_native",
  "version": "1.0.0",
  "main": "index.node",
  "scripts": {
    "build": "cargo build --release && cp ../target/release/libpluribit_native.so ./index.node"
  },
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}
